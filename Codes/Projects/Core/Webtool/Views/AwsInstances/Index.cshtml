@model IEnumerable<Webtool.Models.AwsInstance>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AwsInstanceID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
                <th>
                    @Html.DisplayNameFor(model => model.Region)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Environment)
            </th>

                <th>
                    @Html.DisplayNameFor(model => model.InstanceState)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AwsInstanceID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Environment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InstanceState)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AwsInstanceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AwsInstanceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AwsInstanceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
